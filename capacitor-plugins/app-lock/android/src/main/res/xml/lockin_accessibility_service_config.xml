<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately minimal: only the one event type Lock In Mode needs, and
  canRetrieveWindowContent is off because this feature never reads what's
  on screen — it only ever looks at which package just came to the
  foreground.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="100"
    android:canRetrieveWindowContent="false"
    android:description="@string/applock_service_description" />
