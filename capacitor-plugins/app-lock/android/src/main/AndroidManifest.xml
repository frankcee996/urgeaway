<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.urgeaway.applock">

    <!-- Package-visibility exception (Android 11+): declaring this exact
         intent signature is enough to see other apps' launcher entries via
         PackageManager#queryIntentActivities(), without the sweeping
         QUERY_ALL_PACKAGES permission. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application>
        <activity
            android:name="com.urgeaway.applock.LockInBlockActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <service
            android:name="com.urgeaway.applock.AppLockAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:label="UrgeAway Lock In Mode">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice.accessibility_service"
                android:resource="@xml/lockin_accessibility_service_config" />
        </service>
    </application>
</manifest>
