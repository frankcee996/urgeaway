/* Bundled by esbuild during the CI build into js/firestore-bundle.js.
   See .github/workflows/build-apk.yml. */
import { FirebaseFirestore } from '@capacitor-firebase/firestore';

window.CapFirestore = { FirebaseFirestore };
